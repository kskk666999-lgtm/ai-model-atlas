# 方法论 · METHODOLOGY

本页与网站内"方法论"页内容一致，说明榜单全部计算规则。所有规则公开且确定性，没有任何黑盒评分或大模型参与。

## 1. 数据来源等级

| 等级 | 定义 | 默认综合权重 |
|---|---|---|
| A | 官方基准仓库 / 官方结构化结果 / 官方 API（有版本、提交或可复现材料） | 1.0 |
| B | 官方排行榜导出（官方生成、复现材料不完整） | 0.8 |
| C | 有公开方法论的独立第三方评测 | 0.6 |
| D | 厂商自报 / 无法验证 | 0（默认综合榜不使用） |

## 2. 模型名称规范化

- 各来源的模型名通过**显式别名表**（`data/registry/aliases.yml` 与 `models.yml` 的 aliases 字段）映射到注册表 `canonical_id`
- 仅做无损规范化（小写、去空白、NFKC），**不做相似度猜测合并**
- 未命中别名的名称：不丢弃、不合并，以来源原始名称出现在官方原始榜，并写入 `data/reports/unmapped-models.json` 等待人工补充
- **Thinking / Non-Thinking、reasoning effort（high/xhigh/max）、发布日期、参数量版本一律是独立条目**，绝不为了榜单整洁而错误合并

## 3. Agent 系统与基础模型

- 每条成绩带 `evaluation_target_type` 标签（base_model / api_endpoint / model_plus_agent / embedding_model 等）
- SWE-bench 等系统级成绩（模型+Agent 框架+推理预算）**不进入基础模型综合指数**，在软件工程榜单独展示并显著标注
- Agent 榜与基础模型榜默认分开；同一模型不同 Agent 框架的运行分别列出

## 4. 官方原始榜

- 完全按单个来源与基准展示官方原始分，保留来源等级、评测日期、Agent 框架、基准版本等全部溯源字段
- 分数并列时采用 competition ranking（1, 1, 3），界面对并列成员统一标注"并列·差异可能不显著"

## 5. 综合指数（本站计算）

```
1. benchmark_percentile(m, b)
     = 模型 m 在基准 b 内的百分位（0~100，并列取平均；单模型基准记 50）
     百分位池 = 已映射模型；未映射名称不参与，也不被 0 分替代
2. source_score(m, s) = mean(benchmark_percentile(m, b))，b ∈ 来源 s
3. capability_score(m)
     = Σ_s source_score(m, s) × weight(s) / Σ_s weight(s)
     weight: A=1.0, B=0.8, C=0.6；先在来源内对基准取平均，避免单一来源多基准刷权重
4. overall_index(m)
     = Σ_c capability_score(m, c) × w(c) / Σ_c w(c)
     只统计模型有数据的 c；缺失能力不按 0 计入（权重预设可在前端切换、本地重算）
```

- **综合榜分两级**：「多源验证综合榜（Beta）」只收录覆盖 ≥4 能力类别、≥5 基准、≥2 独立数据来源且全部为非 Agent 成绩的模型，可视为跨来源共识（当前模型较少是规则的诚实结果）；「单源参考综合榜」收录有任意能力数据的全部模型，行内标注"单源"者成绩仅来自一个来源，不可视为多源共识
- 单一来源的能力指数标注"单一来源"，不伪装成多源共识
- 置信度：高 = ≥2 个 A/B 级来源；中 = 多来源含 C 或单 A/B 来源；低 = 单 C 来源
- 每行展示：指数、原始百分位、基准覆盖数、来源数、最近更新时间、是否并列

## 6. 缺失数据

- 缺失一律显示"— / 暂无数据"，**永远不当作 0 分**；只影响覆盖率与置信度展示
- 价格未知的模型不进入价格受限的推荐与价格榜

## 7. 价格与性价比

- 输入 / 输出价格分列（USD / 1M tokens），来源为 LiveBench 官方统计（可选叠加 Artificial Analysis）
- 性价比 = 对应能力指数 ÷ 价格，浏览器本地计算，公式公开
- 标价 ≠ 实际账单（输出长度、缓存、推理 Token 等均影响实际成本）

## 8. 历史排名

- 每日快照：`data/history/snapshots/<date>.json`
- 保留策略：90 天内每日保留；90 天~2 年按 ISO 周聚合；超过 2 年按月聚合
- 7 天 / 30 天排名变化取"不早于 N 天前的最近一份快照"为基线
- 模型从来源消失时不删除历史，前台提示"当前榜单缺席"

## 9. 数据安全与失败策略

- 来源级错误隔离：单来源失败自动回退 Last Known Good（`data/cache/records/`），状态标记"降级"
- 核心 Schema 校验失败 → 流水线退出码 2，不触碰 `public/data/`
- 更新只在内容变化时写入（内容 Hash 对比 + 原子替换）
- 生产构建拒绝 `DEMO_MODE=true`；`public/data` 中不存在任何模拟分数

## 10. 网站局限性

- 百分位指数衡量相对位置，跨能力的指数绝对值不可直接比较
- 评测分数反映评测时点的端点行为；厂商更新后历史分数不重算
- 覆盖来源少的模型指数波动更大，请结合置信度解读
- 暂无可信数据源的能力（人类偏好、长上下文、Agent 通用执行、工具调用、图像/视频生成、语音、安全性、鲁棒性）显示"数据接入中"
